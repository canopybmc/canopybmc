name: "Validator: Redfish EventService"
description: >-
  Validate /redfish/v1/EventService tree against DMTF schemas.
on-error: continue

defaults:
  transport: &local
    proto: local

stages:
  - name: Validate EventService
    steps:
      - cmd: cmd
        name: Check storage provisioning
        transport: *local
        parameters:
          executable: ls
          args:
            - "-la"
            - "[[storage.RedfishValidator]]"
      - cmd: cmd
        name: Validate EventService tree
        transport: *local
        options:
          timeout: 5m
          on-error: continue
        parameters:
          executable: rf_service_validator
          args:
            - "--user"
            - "root"
            - "--password"
            - "[[attributes.BMCPassword]]"
            - "--rhost"
            - "https://[[attributes.BMC]]"
            - "--payload"
            - "tree"
            - "/redfish/v1/EventService"
            - "--nooemcheck"
            - "--logdir"
            - "/tmp/redfish-results-events"
      - cmd: cmd
        name: Show validation report
        transport: *local
        parameters:
          executable: cat
          args: ["/tmp/redfish-results-events/RedfishServiceValidatorReport_latest.html"]
